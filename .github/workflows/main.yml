name: Sync Fork

on:
  schedule:
    - cron: '*/1440 * * * *' # every 24 hours
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.8
        with:
          base: main
          head: main
